import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Widget tests will be added per screen. This file exists so
    // `flutter test` has at least one target.
    expect(1 + 1, 2);
  });
}
